dmz:
  hosts:
    gman.local:

kube_control_plane:
  hosts:
    wheatley.local:

kube_worker:
  hosts:
    glados.local:

kube_node:
  children:
    kube_control_plane:
    kube_worker:

cloud:
  hosts:
    {}
    # TODO
    # vance.local:
